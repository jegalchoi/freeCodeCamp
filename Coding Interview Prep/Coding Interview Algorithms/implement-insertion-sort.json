{
  "index.js": "function insertionSort(arr) {\n  // change code below this line\n  let copyArr = arr.slice();\n  console.log(copyArr);\n\n  let sortedArr = [];\n  sortedArr.push(copyArr.shift());\n  \n  for (let i = 0; i < arr.length; i += 1) {\n    console.log(`sortedArr at beginning: ${sortedArr}`);\n    if (copyArr.length == 0) {\n      break;\n    };\n\n    let element = copyArr.shift();\n    console.log(element);\n\n    for (let j = 0; j < sortedArr.length + 1; j += 1) {\n      if (element <= sortedArr[j]) {\n        sortedArr.splice(j, 0, element);\n        break;\n      }; \n      if (j == sortedArr.length)  {\n        sortedArr.push(element);\n        break;\n      };\n    } \n  console.log(copyArr);  \n  console.log(`sortedArr at end: ${sortedArr}`);\n  };\n  \n  return sortedArr;\n  // change code above this line\n}\n\ninsertionSort([1, 4, 2, 8, 345, 123, 43, 32, 5643, 63, 123, 43, 2, 55, 1, 234, 92]);\n"
}