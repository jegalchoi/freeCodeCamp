{
  "index.js": "function rot13(str) { // LBH QVQ VG!\n  let a = \"ABCDEFGHIJKLMNOPQRSTUVWXYZ\"\n  let alphabet = a.split(\"\");\n  console.log(alphabet);\n\n  let ascii = [];\n  for (let i = 97; i < 123; i += 1) {\n    ascii.push(i);\n  };\n  console.log(ascii);\n\n  let alphaToAscii = {};\n  for (let i = 0; i < ascii.length; i += 1) {\n    alphaToAscii[alphabet[i]] = ascii[i];\n  }\n  console.log(alphaToAscii);\n\n  let asciiToAlpha = {};\n  for (let i = 0; i < ascii.length; i += 1) {\n    asciiToAlpha[ascii[i]] = alphabet[i];\n  }\n  console.log(asciiToAlpha);\n\n  let newStr = \"\";\n\n  for (let i = 0; i < str.length; i += 1) {\n    if (alphaToAscii[str[i]] == undefined) {\n      newStr = newStr.concat(str[i]);\n    } else {\n      newStr = newStr.concat(asciiToAlpha[(alphaToAscii[str[i]] + 13) % 97 % 26 + 97]);\n    }\n    \n  };\n\n  return newStr;\n}\n\n// Change the inputs below to test\nrot13(\"SERR PBQR PNZC\");\n"
}